on: push

jobs:
    npm-build:
        name: npm-build
        runs-on: ubuntu-latest
        
        steps:
            - name: Read repo
              uses: actions/checkout@v4

            - name: Install dependencies
              run: npm install
              
            - name: Build project
              run: npm run build